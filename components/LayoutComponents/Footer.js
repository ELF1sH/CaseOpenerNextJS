import styles from '../../styles/Footer.module.scss'

const Footer = () => {
    return (
        <footer className={styles.footer}>
            This is a footer 🤓
        </footer>
    )
}

export default Footer